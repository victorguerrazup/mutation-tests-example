/// <reference types="node" />
import { StrykerOptions, INSTRUMENTER_CONSTANTS } from '@stryker-mutator/api/core';
import { Logger } from '@stryker-mutator/api/logging';
import { Injector, PluginContext } from '@stryker-mutator/api/plugin';
import { TestRunner, MutantRunOptions, DryRunResult, MutantRunResult, DryRunOptions } from '@stryker-mutator/api/test-runner';
import { JestTestAdapter } from './jest-test-adapters/jest-test-adapter';
import { JestConfigLoader } from './config-loaders/jest-config-loader';
export declare function createJestTestRunnerFactory(namespace?: typeof INSTRUMENTER_CONSTANTS.NAMESPACE | '__stryker2__'): {
    (injector: Injector<PluginContext>): JestTestRunner;
    inject: ['$injector'];
};
export declare const jestTestRunnerFactory: {
    (injector: Injector<PluginContext>): JestTestRunner;
    inject: ['$injector'];
};
export declare class JestTestRunner implements TestRunner {
    private readonly log;
    private readonly processEnvRef;
    private readonly jestTestAdapter;
    private readonly globalNamespace;
    private readonly jestConfig;
    private readonly jestOptions;
    private readonly enableFindRelatedTests;
    static inject: ["logger", "options", "processEnv", "jestTestAdapter", "configLoader", "globalNamespace"];
    constructor(log: Logger, options: StrykerOptions, processEnvRef: NodeJS.ProcessEnv, jestTestAdapter: JestTestAdapter, configLoader: JestConfigLoader, globalNamespace: typeof INSTRUMENTER_CONSTANTS.NAMESPACE | '__stryker2__');
    dryRun({ coverageAnalysis, disableBail, files, }: Pick<DryRunOptions, 'coverageAnalysis' | 'disableBail' | 'files'>): Promise<DryRunResult>;
    mutantRun({ activeMutant, sandboxFileName, testFilter, disableBail }: MutantRunOptions): Promise<MutantRunResult>;
    private configForDryRun;
    private configForMutantRun;
    private configWithRoots;
    private run;
    private collectRunResult;
    private collectSerializableErrorText;
    private setEnv;
    private processTestResults;
    private mergeConfigSettings;
}
//# sourceMappingURL=jest-test-runner.d.ts.map