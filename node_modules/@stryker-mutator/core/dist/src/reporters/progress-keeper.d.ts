import { MutantResult, MutantTestCoverage } from '@stryker-mutator/api/core';
import { Reporter } from '@stryker-mutator/api/report';
export declare abstract class ProgressKeeper implements Reporter {
    private timer;
    protected progress: {
        survived: number;
        timedOut: number;
        tested: number;
        total: number;
    };
    onAllMutantsMatchedWithTests(mutants: readonly MutantTestCoverage[]): void;
    onMutantTested(result: MutantResult): void;
    protected getElapsedTime(): string;
    protected getEtc(): string;
    private formatTime;
}
//# sourceMappingURL=progress-keeper.d.ts.map