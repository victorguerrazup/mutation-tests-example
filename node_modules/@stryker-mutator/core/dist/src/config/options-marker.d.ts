import type { JSONSchema7 } from 'json-schema';
import { StrykerOptions } from '@stryker-mutator/api/core';
import { Logger } from '@stryker-mutator/api/logging';
/**
 * Performs additional validation on the Stryker options to mark unusual behavior with a warning.
 * Namely when a value isn't serializable or when unknown options are passed in.
 */
export declare function markOptions(options: StrykerOptions, schema: JSONSchema7, log: Logger): StrykerOptions;
export declare namespace markOptions {
    var inject: ["options", "validationSchema", "logger"];
}
//# sourceMappingURL=options-marker.d.ts.map