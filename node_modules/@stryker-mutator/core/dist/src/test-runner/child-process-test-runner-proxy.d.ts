import { StrykerOptions } from '@stryker-mutator/api/core';
import { Logger } from '@stryker-mutator/api/logging';
import { TestRunner, DryRunOptions, MutantRunOptions, MutantRunResult, DryRunResult } from '@stryker-mutator/api/test-runner';
import { LoggingClientContext } from '../logging';
/**
 * Runs the given test runner in a child process and forwards reports about test results
 */
export declare class ChildProcessTestRunnerProxy implements TestRunner {
    private readonly log;
    private readonly worker;
    constructor(options: StrykerOptions, sandboxWorkingDirectory: string, loggingContext: LoggingClientContext, log: Logger);
    init(): Promise<void>;
    dryRun(options: DryRunOptions): Promise<DryRunResult>;
    mutantRun(options: MutantRunOptions): Promise<MutantRunResult>;
    dispose(): Promise<void>;
}
//# sourceMappingURL=child-process-test-runner-proxy.d.ts.map