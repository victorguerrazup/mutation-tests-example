"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MaxTestRunnerReuseDecorator = void 0;
const test_runner_decorator_1 = require("./test-runner-decorator");
/**
 * Wraps a test runner and implements the retry functionality.
 */
class MaxTestRunnerReuseDecorator extends test_runner_decorator_1.TestRunnerDecorator {
    constructor(testRunnerProducer, options) {
        super(testRunnerProducer);
        this.runs = 0;
        this.restartAfter = options.maxTestRunnerReuse || 0;
    }
    async mutantRun(options) {
        this.runs++;
        if (this.restartAfter > 0 && this.runs > this.restartAfter) {
            await this.recover();
            this.runs = 1;
        }
        return super.mutantRun(options);
    }
    dispose() {
        this.runs = 0;
        return super.dispose();
    }
}
exports.MaxTestRunnerReuseDecorator = MaxTestRunnerReuseDecorator;
//# sourceMappingURL=max-test-runner-reuse-decorator.js.map