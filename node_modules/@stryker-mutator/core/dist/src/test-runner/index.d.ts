import { TestRunner } from '@stryker-mutator/api/test-runner';
import { StrykerOptions } from '@stryker-mutator/api/core';
import { LoggerFactoryMethod } from '@stryker-mutator/api/logging';
import { LoggingClientContext } from '../logging';
import { Sandbox } from '../sandbox/sandbox';
export declare function createTestRunnerFactory(options: StrykerOptions, sandbox: Pick<Sandbox, 'workingDirectory'>, loggingContext: LoggingClientContext, getLogger: LoggerFactoryMethod): () => TestRunner;
export declare namespace createTestRunnerFactory {
    var inject: ["options", "sandbox", "loggingContext", "getLogger"];
}
//# sourceMappingURL=index.d.ts.map